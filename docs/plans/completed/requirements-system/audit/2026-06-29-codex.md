---
audit-of: requirements-system
auditor: codex
date: 2026-06-29
verdict: NOT READY
blocking-issues: 7
advisory-issues: 3
---

# Plan Audit: requirements-system

## Verdict

NOT READY

**Reason:** The plan names the right problem, but the sprint files do not yet force a complete source corpus, deterministic verification, gate coverage, or executable handoffs.

## Blocking issues (must fix before activation)

| # | Sprint | Issue | Evidence | Fix |
|---|---|---|---|---|
| 1 | RS-R1, RS-R4, README | Session logs are not a first-class source, even though the triggering failure was requirements and decisions being buried in session evidence. | README lines 59-69 makes the inaugural dataset the 2026-06-29 session corpus, but only names three on-hold output files. RS-R1 lines 11-14 lists CSV, product docs, archive, and FP decisions, but not `docs/progress/sessions/**`, `docs/archive/sessions/**`, or `docs/progress/index.csv`. Claude's own log `2026-06-29-claude/04-requirements-recovery-reconciliation.md` records the missing-requirements discovery. | Add a mandatory source-corpus section: current session logs, relevant archived session logs, `docs/progress/index.csv`, on-hold outputs, plan audits/output-reviews, CSV, product docs, decisions/open questions, and v0.1.4 archive. RS-R1 must output a source manifest with included/excluded paths and reasons. |
| 2 | RS-R1 | "Read all sources" is not executable and does not define how completeness is proven. | RS-R1 line 9 says "Read all sources"; acceptance line 24 says "every source requirement" without a command, manifest, or count source. The repo contains `dcx-requirements-master.csv`, product requirement docs, on-hold outputs, and archived sessions. | Require deterministic inventory commands, for example `find`/`rg --files` path lists and CSV row counts, and require the output to include a source manifest, row/document counts, and gaps. |
| 3 | README, all RS sprints | Carry-forward contract exists only as a placeholder, so the multi-sprint plan violates `core.md §27`. | README lines 110-113 says it will be populated later. RS-R0 final step updates it, but RS-R1-R6 Step 0 do not explicitly read the carry-forward contract plus previous sprint output, and each final step is vague. | Populate an initial contract before activation with canonical source homes, on-hold evidence, pending unknowns, and "do not recreate" rules. Add Step 0 and final carry-forward updates to every sprint. |
| 4 | RS-R2, RS-R3, RS-R4, RS-R5 | Code/documentation-modifying sprints lack full gate coverage and exact commands. | RS-R2 line 29 lists typecheck/lint/test but omits `validate:architecture`; RS-R3/RS-R4 have validators only; RS-R5 edits `core.md`/`AGENTS.md` but lists no typecheck/lint/test/verify/index gates. Package scripts available include `typecheck`, `lint`, `validate:architecture`, `test`, and `verify:frontend`. | For every sprint that creates tooling or edits governance docs, list exact commands: `npm run typecheck`, `npm run lint`, `npm run validate:architecture`, `npm run test`, `bash scripts/verify.sh`, plus the new requirement-system validator once RS-R2 creates it. If a gate is N/A, state why. |
| 5 | RS-R0, RS-R2, RS-R3, RS-R4 | Tool-dependent criteria have no fallback path, contrary to `core.md §28`. | RS-R2 lines 14-18 invents future `validate`, `index/generate-views`, `propose-change`, and `apply-after-signoff`; RS-R3 line 22 and RS-R4 line 27 depend on validators passing. No sprint defines what to do if a script is absent, fails, or is not yet wired into `npm run`. | Add a fallback column for every future tool: direct script path, documented manual inspection, or `BLOCKED` logging. RS-R2 must install/declare exact command names before RS-R3/RS-R4 can depend on them. |
| 6 | RS-R5 | Doc disposition acceptance points to the wrong rule and is not safely executable. | RS-R5 line 25 says removed legacy docs are archived per `core.md §32`, but §32 is evidence/screenshot paths, not document archival. Lines 15-17 list broad doc groups but not exact files or an archive destination. | Replace the rule reference with the correct archive/disposition policy or define one in RS-R0/RS-R5. Require a file-by-file disposition table and PO approval record before any move/remove. |
| 7 | RS-R6 | The sprint mixes handoff preparation with redoing FP-R4/FP-R5 and PO reactivation, which violates the on-hold lifecycle boundary. | RS-R6 lines 11-15 includes producing a brief, PO moving the on-hold plan back to active, and "Redo FP-R4 + FP-R5"; core.md §24 says agents must not resume or re-audit an on-hold plan until the PO reactivates it. | Split RS-R6 into a pure re-grounding brief sprint. Make PO reactivation a dependency after RS-R6, then let the reactivated frontend-polish plan own FP-R4/FP-R5 redo. |

## Advisory issues (should fix, won't block)

| # | Sprint | Issue | Evidence | Suggested fix |
|---|---|---|---|---|
| 1 | README | `executor: TBD` at plan level weakens routing. | README line 9 is TBD while sprint files name broad alternatives. | Name a recommended executor per sprint in the README index; keep alternatives in sprint files. |
| 2 | RS-R0 | The option list risks reintroducing markdown-first design. | RS-R0 lines 15-17 lists structured YAML/JSON + generated spreadsheet + docs, and CSV/SQLite options. | Add an explicit evaluation criterion: human edit surface must be primary or losslessly generated from canonical data, not agent-only markdown. |
| 3 | RS-R0 | The local `dcx-plan-audit` skill is not installed for Codex. | `.agents/skills/` only contains `impeccable`; canonical `agent-skills/dcx-plan-audit/SKILL.md` exists. | Run the project skill sync before activation or document that agents use canonical skill fallbacks when local skill resolution fails. |

## Prior art compliance

No expired plan directly covers the requirements-system scope. The plan's relevant prior art is the on-hold `frontend-polish-v0.3.5` evidence:

- `output/requirements-recovery.md` proves the previous discovery missed `dcx-requirements-master.csv` and the v0.1.4 archive, and documents recovered keyboard, copy/paste, deselect, STG/DZ/SBC/FCS/RDY/IFX/KBI families.
- `output/core-interaction-model.md` captures PO-aligned builder behavior decisions: version-readiness rollup, smart default expansion, auto-centre on select, drag-pill creation, and DnD as foundational.
- `output/decision-register.md` captures D-01 through D-12, including the refined D-02 highlight/default plus opt-in FCS-002 isolation.

The README incorporates those outputs at a high level, but the sprint files do not yet guarantee they will be reconciled against session logs, archived logs, output reviews, audits, and progress indexes. That is the biggest readiness gap.

## Gate coverage summary

| Sprint | typecheck | lint | validate:architecture | test | browser | Notes |
|---|---|---|---|---|---|---|
| RS-R0 | N/A stated implicitly | N/A stated implicitly | N/A | N/A | N/A | Design-only, but needs exact "no src change" evidence and PO sign-off record. |
| RS-R1 | Missing | Missing | Missing | Missing | N/A | Discovery/map sprint; should at least run path/source manifest checks and log no `src/` change. |
| RS-R2 | Listed | Listed | Missing | Listed | N/A | Tooling sprint; must add architecture + verify gates and exact command names. |
| RS-R3 | Missing | Missing | Missing | Missing | N/A | Migration/data sprint depends on validators but lacks standard gates. |
| RS-R4 | Missing | Missing | Missing | Missing | N/A | Dogfood data-ingest sprint depends on validators + PO sign-off but lacks standard gates. |
| RS-R5 | Missing | Missing | Missing | Missing | N/A | Governance/doc-disposition sprint edits agent rules and needs verification + index rebuild. |
| RS-R6 | N/A unclear | N/A unclear | N/A unclear | N/A unclear | N/A | Should be handoff-only; current scope crosses into on-hold plan execution. |

## Handoff quality

The handoff is not yet safe for opencode or a future Codex run. Output filenames are named, but section structure is mostly undefined. RS-R1 does not define the exact reconciliation table columns. RS-R2 does not define the command names that later sprints must run. RS-R3 does not define the populated-system path or ledger format because it depends on RS-R0, but it also lacks a placeholder contract saying where RS-R0 must record those decisions. RS-R6 asks agents to redo another plan's FP-R4/FP-R5 while that plan is still on hold.

## Ready checklist

- [ ] All blocking issues resolved
- [ ] Prior art findings incorporated
- [ ] Every sprint has executor named
- [ ] Every code-modifying sprint has gate coverage
- [ ] Session start steps present in each sprint
- [ ] (2+ sprints) Carry-forward contract present; every sprint reads it (Step 0) and updates it (final step)
- [ ] Tool-dependent criteria have a documented fallback (core.md §28)
