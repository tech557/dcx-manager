---
audit-of: folder-structure-v2
auditor: codex
date: 2026-06-27
verdict: READY
blocking-issues: 0
advisory-issues: 2
method: dcx-plan-audit skill; final current-plan readback; live src grep counts; prior-art and UX2/FE2/BE2/product cross-check
supersedes: 2026-06-27-codex prior NEEDS REVISION audit
---

# Plan Audit: folder-structure-v2

## Verdict

**READY**

**Reason:** The final draft resolves the prior token-scope, component-premise, backend-scope, and product-gate blockers; remaining issues are wording/tooling advisories only.

## Blocking issues (must fix before activation)

| # | Sprint | Issue | Evidence | Fix |
|---|---|---|---|---|
| — | — | None. | Final readback confirms the previous blockers were resolved in README, P1, P2, P4, and P5. | No blocker before activation. |

## Advisory issues (should fix, won't block)

| # | Sprint | Issue | Evidence | Suggested fix |
|---|---|---|---|---|
| 1 | P5 | One output-template placeholder still says "section IDs" for the visual quality gate, although the sprint body now correctly says the Quality Gates section has no `BLD-*` ID and must be cited by anchor. This is no longer blocking because the operative instruction is explicit. | P5 Product requirements section: cite `acceptance-criteria.md#quality-gates`, write `Quality gates: no requirement ID assigned in source doc`, and do not invent a `BLD-*` ID. Later output template says `{cite docs/product/requirements/builder/acceptance-criteria.md section IDs...}`. | Optional polish: change the template placeholder to `{cite acceptance-criteria.md#quality-gates; record no BLD ID assigned}`. |
| 2 | Process / tooling | `build-log-index.sh` still mislabels Codex logs by using a later `## Session Environment` heading as the sprint id. This is documented and not part of folder-structure-v2 execution. | Current README follow-up logs the issue; this session again required manual index-row correction after rebuild. | Fix `scripts/build-log-index.sh` in a separate tooling task so it uses the first markdown heading only, or the filename, for sprint id. |

## Prior art compliance

Compliant. The plan incorporates the relevant expired prior art rather than rediscovering it:

- `expired/src-structure-refactor`: keeps Tailwind-first direction, rejects CSS modules, keeps real backend integration out of this plan, preserves AI/ClickUp stubs, and treats `mockDispatch`/`src/mock` as the dev/mock layer until the later `production-api-client-switch`.
- `expired/src-structure-audit`: keeps dependency-cruiser/layer verification, respects `src/ui` vs `src/builder` boundaries, and does not reintroduce the old `src/components` ambiguity.
- UX2 discovery: P1 covers all required token-cleanup inputs: 11 typography vars, 26 raw hex values, 6 missing surface/accent tokens, 3 dead CSS classes, 3 dead token exports, and explicit dispositions for retained theme/shadow/radius/layout arbitrary values.
- FE2 discovery: P2 recognizes existing `Badge`, `Chip`, `Input`, and `ToggleGroup`; deletes true orphans safely with barrel cleanup; keeps context-coupled components in place; P3 covers editor-hook/quality findings.
- BE2 discovery: P4 targets mock API seam readiness, wires the 8 localStorage services through `apiClient -> mockDispatch`, labels AI/ClickUp as stubs, and leaves production integration to a named follow-up.

Live verification snapshot remains consistent with the plan's stated scope:

| Pattern group | Live count | Plan disposition |
|---|---:|---|
| `text-[var(--text-*)]` | 275 | P1 migrates all 11 variants to `text-dcx-*`. |
| `(text/bg/border/ring)-[var(--theme-*)]` | 287 | Intentionally retained as theme-reactive; optional future `P1b-color-tokens`. |
| `font-[var(--font-*)]` | 0 | Phantom category; no migration. |
| `shadow-[var(--shadow-*)]` | 0 | Phantom category; no migration. |
| `rounded-[var(--radius-*)]` | 0 | Phantom category; no migration. |

## Gate coverage summary

| Sprint | typecheck | lint | validate:architecture | test | browser | Notes |
|---|---|---|---|---|---|---|
| P1-token-system | Listed | Listed | Listed | Listed | Listed | Covers typography, raw hex, missing tokens, dead CSS, dead exports, and browser spot-check. |
| P2-component-consolidation | Listed | Listed | Listed | Listed | Listed | Includes safe orphan deletion, barrel cleanup, existing atom reconciliation, and component screenshots. |
| P3-structure-quality | Listed | Listed | Listed | Listed | Listed | Depends on P2 output; covers editor hooks, stale closures, drag state, unused hooks, and `any` reduction. |
| P4-backend-readiness | Listed | Listed | Listed | Listed | Listed | Covers mock API seam, 8 services, attach-file persistence, completeness matrix, and retained mock layer. |
| P5-frontend-readiness | Listed | Listed | Listed | Listed | Listed | Covers source policy, shadcn adapter seam, Quality Gates citation by anchor, and 1440/1920/2560 polish evidence. |

## Handoff quality

The handoff is sufficient for activation. Each sprint has:

- a named executor,
- explicit Step 0 environment logging,
- concrete inputs and output path,
- deterministic checks or grep commands for the risky parts,
- code gates before sign-off,
- browser/dev-server evidence where UI/runtime behavior is touched.

The plan now covers all three discovery streams and the user's clarified scope: frontend readiness for systematic polish and component-source switching, plus backend mock-seam readiness without pretending production integration is complete.

## Ready checklist

- [x] All blocking issues resolved.
- [x] Prior art findings incorporated.
- [x] Every sprint has executor named.
- [x] Every code-modifying sprint has gate coverage.
- [x] Session start steps present in each sprint.
- [x] UX2, FE2, and BE2 discovery inputs represented in the plan.
- [x] Backend scope correctly limited to mock API seam readiness; production integration deferred.
- [x] P5 frontend polish/component-source readiness is explicit and executable.

