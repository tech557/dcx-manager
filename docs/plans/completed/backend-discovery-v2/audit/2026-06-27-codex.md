---
audit-of: backend-discovery-v2
auditor: codex
date: 2026-06-27
verdict: READY
blocking-issues: 0
advisory-issues: 2
resolution-round: 7
---

# Plan Audit: backend-discovery-v2

## Verdict

READY

**Reason:** Claude resolved the final BE2-R3 classifier blocker: infrastructure files and transport envelope `Api*` types are now excluded from domain mapper coverage.

## Blocking issues (must fix before activation)

None.

## Advisory issues (should fix, won't block)

| # | Sprint | Issue | Evidence | Suggested fix |
|---|---|---|---|---|
| 1 | BE2-R2 | Step 3 still labels a service `fetch/api` when it merely references `apiClient`, including possible seam/stub references. Step 5 separately checks whether `apiClient()` is wired, so this remains manageable. | BE2-R2 uses `grep -c 'fetch\|apiClient\|axios'` for transport classification. | Rename the label to `references API seam` and keep Step 5 as the source of truth for wired/stub/throwing status. |
| 2 | BE2-R3 | `logs.service.ts` is included in `INFRA_FILES`, so Step 3 prints `mapper N/A` instead of proving `ApiActivityEvent` mapper coverage. This does not hide a P4 blocker because the script still reports error handling/apiClient status, and same-shape Scenario A can still be assessed. | Probe output: `src/services/logs.service.ts: INFRA (mapper N/A) | error handling: False | apiClient: False`. Expired BE-R2 treats logs as a service, not infrastructure. | Prefer removing `logs.service.ts` from `INFRA_FILES` so the output records `ApiActivityEvent -> apiActivityEventToDomain`; keep `api-client.ts` and `api-mappers.ts` as true infrastructure. |

## Verification probes

- The old shared-mapper false-positive is resolved: BE2-R3 no longer uses the global `has_mapper_shared` shortcut.
- The transport-envelope false-positive is resolved: `api-client.ts` is classified as `INFRA (mapper N/A)`, so `ApiClientRequestOptions` and `ApiClientResponse` no longer produce fake "needs mapper" blockers.
- The revised classifier remains entity-specific for normal service return types:
  - `builder.service.ts`: `ApiBuilderTree` maps through `apiBuilderTreeToDomain` in `builder.queries.ts`; `ApiPhase` maps through `apiPhaseToDomain` in `api-mappers.ts`.
  - `channels.service.ts`: `ApiChannel` and `ApiChannelComposition` map through `channels.queries.ts`.
  - `versions.service.ts`: `ApiVersion` maps through `versions.queries.ts`.
  - `subtask-definitions.service.ts`: `ApiSubtaskDefinition` maps through `subtask-definitions.queries.ts`.
  - `files.service.ts`: `ApiFileAttachment` and `ApiVersion` mapper coverage is found in `api-mappers.ts`.
- Current session evidence: repository version `v0.3.4`; active plans `[]`; MCP operational `eslint`; MCP awaiting setup `storybook`, `shadcn`, `semgrep`, `sonarqube`; code index fresh by `verify-tooling-state.sh`; Semgrep CLI and Storybook still not installed.

## Prior art compliance

The backend plan incorporates the expired backend discovery findings:

- BE2-R1 uses expired BE-R1 and the P4 scope from `src-structure-refactor`.
- BE2-R2 uses expired BE-R2's service-readiness baseline.
- BE2-R3 uses expired BE-R3's Scenario A premise: services swap to fetch, while mappers, queries, actions, guards, and helpers survive if the API shape stays compatible.

The prior art's key backend findings are preserved: type duplication is measured with the corrected semantic-pair script, service localStorage/API readiness is remeasured, and BE2-R3 produces the exact P4 integration fix list.

## Gate coverage summary

| Sprint | typecheck | lint | validate:architecture | test | browser | Notes |
|---|---|---|---|---|---|---|
| BE2-R1 | Audit subject | Audit subject | N/A | N/A | N/A | Discovery sprint, no source changes. |
| BE2-R2 | N/A | N/A | N/A | N/A | N/A | Discovery sprint, no source changes. |
| BE2-R3 | N/A | N/A | N/A | N/A | N/A | Discovery sprint, no source changes. |

## Handoff quality

BE2-R1, BE2-R2, and BE2-R3 are now executable as discovery/synthesis sprints. BE2-R3's output should be sufficient for folder-structure-v2 P4 because it now separates real service/domain mapper coverage from transport infrastructure and still reports the concrete blockers that matter for the backend swap: missing error handling, missing `apiClient` wiring, service stubs, and exact type cleanup.

## Ready checklist

- [x] All blocking issues resolved
- [x] Prior art findings incorporated
- [x] Every sprint has executor named
- [x] Every code-modifying sprint has gate coverage
- [x] Session start steps present in each sprint
