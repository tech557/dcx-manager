---
audit-of: backend-discovery-v2
auditor: codex
date: 2026-06-26
verdict: NEEDS REVISION
blocking-issues: 1
advisory-issues: 2
resolution-round: 4
resolved: 2026-06-27
resolved-by: claude
resolution-verdict: READY
---

> **Resolution note round 4 (2026-06-27):** Final blocker resolved.
>
> B1 (BE2-R3 Step 3 "needs mapper" checks only the service file body): The check now checks the
> service/query boundary — it looks in the service file, then `src/services/api-mappers.ts`, then
> any `src/queries/` file that imports the service. If mapper signals are found at any of these three
> locations, the service is classified as `mapper covered (in service/queries/shared)` rather than
> blocked. The output prints the mapper location for each service so the P4 fix list is precise.
>
> Advisory A1 (BE2-R1 acceptance criterion still mentioned `comm -12`): Updated to reference the
> Python identifier/semantic-pair script.

# Plan Audit: backend-discovery-v2

## Verdict

NEEDS REVISION

**Reason:** Claude fixed BE2-R1's duplicate-type measurement and BE2-R3's repo-relative paths. One blocker remains in BE2-R3: the service readiness classifier still marks "needs mapper" by looking only inside service files, even though this architecture can map at the query/API-boundary layer. That can produce a wrong folder-structure-v2 P4 fix list.

## Blocking issues

| # | Sprint | Issue | Evidence | Fix |
|---|---|---|---|---|
| 1 | BE2-R3 | Step 3 marks a service as `needs mapper` whenever the service file lacks `mapper`, `Mapper`, `toApi`, or `fromApi`. In this codebase, services may intentionally return `Api*` shapes and mapping may occur in `queries/` or `src/services/api-mappers.ts`. Because BE2-R3's output becomes the exact P4 fix list, this can over-scope mapper work and mislabel ready services as blocked. | BE2-R3 Step 3 checks only each `src/services/*.ts` file. The expired BE-R3 premise says mappers/queries/actions/guards/helpers survive Scenario A, so the readiness check must verify the service boundary, not only the service file body. | Change the blocker to "mapper coverage missing at service/query boundary" and check both the service and its query/action consumers, plus `src/services/api-mappers.ts`, before adding `needs mapper`. If the architecture intentionally maps outside services, classify it as `mapper covered outside service` instead of blocked. |

## Advisory issues

| # | Sprint | Issue | Evidence | Suggested fix |
|---|---|---|---|---|
| 1 | BE2-R1 | The acceptance criteria still says "duplication count from `comm -12`" even though Step 5 was correctly replaced with the Python identifier/semantic-pair script. | BE2-R1 Step 5 uses Python and reports exact plus `Api`-prefix semantic pairs; acceptance criteria still mentions `comm -12`. | Update the acceptance criterion text to match the Step 5 Python script. |
| 2 | BE2-R2 | Step 3 classifies a service as `fetch/api` when it references `apiClient`, including possible stub/seam references. Step 5 separately checks whether `apiClient()` is wired, so this is manageable. | BE2-R2 uses `grep -c 'fetch\|apiClient\|axios'` for transport classification. | Label this as "references API seam" and make Step 5 the source of truth for wired/stub/throwing status. |

## Verification probes

- BE2-R1's revised duplicate script now finds the real current signal: `17` API types, `10` domain types, `0` exact matches, `6` semantic `Api`-prefix pairs (`ApiAction/Action`, `ApiDCX/DCX`, `ApiPhase/Phase`, `ApiSubtask/Subtask`, `ApiTask/Task`, `ApiVersion/Version`).
- BE2-R3 now checks repo-relative output paths under `docs/plans/drafted/backend-discovery-v2/output/`.
- BE2-R3 output files are not present yet, so the "do not continue until R1/R2 outputs exist" guard remains appropriate.

## Prior art compliance

The backend plan now correctly states that type deduplication and `draftData` typing were P4 scope, not P1 scope. BE2-R1 reads expired BE-R1 and the `src-structure-refactor` README. BE2-R2 reads expired BE-R2. BE2-R3 reads expired BE-R3.

## Gate coverage summary

| Sprint | typecheck | lint | validate:architecture | test | browser | Notes |
|---|---|---|---|---|---|---|
| BE2-R1 | Audit subject | Audit subject | N/A | N/A | N/A | Discovery sprint, no source changes. |
| BE2-R2 | N/A | N/A | N/A | N/A | N/A | Discovery sprint, no source changes. |
| BE2-R3 | N/A | N/A | N/A | N/A | N/A | Discovery sprint, no source changes. |

Current session evidence: repository version `v0.3.2`; active plans `[]`; MCP operational `eslint`; MCP awaiting setup `storybook`, `shadcn`, `semgrep`, `sonarqube`; code index stale by 1463 minutes.

## Handoff quality

BE2-R1 is now executable and produces the semantic duplicate count folder-structure-v2 needs. BE2-R2 remains usable, with the apiClient classification caveat above. BE2-R3 should not be signed off until its mapper-boundary classifier is corrected, because that sprint is responsible for the exact backend integration fix list.

## Ready checklist

- [ ] All blocking issues resolved
- [x] Prior art findings incorporated
- [x] Every sprint has executor named
- [x] Every code-modifying sprint has gate coverage
- [x] Session start steps present in each sprint
