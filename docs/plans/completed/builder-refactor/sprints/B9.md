# Sprint B9 — Multi-Select Drag Grouping

**Preconditions:** B-CRD complete
**Rollback boundary:** Revert only this sprint's changed files if typecheck fails

> See `docs/plans/active/builder-refactor/agent-execution-guide.md` for the task format and agent execution notes applicable to all tasks.


## B9.1 — Multi-card drag grouping

**Requirements:** BLD-DND-001  
**Files to inspect:** `src/builder/cards/useCardDrag.ts`, `src/actions/phase.actions.ts`, `src/actions/action.actions.ts`, `src/actions/task.actions.ts`  
**Files to change:** `src/builder/cards/useCardDrag.ts`, relevant action files

**Required behaviour:** When dragging a card in a multi-selection: payload already includes `ids: selectedNodeIds`. On drop: if all ids are same kind, move all in preserved visual order. Mixed-level selection: only the dragged card moves.

**Acceptance criteria:**
```
□ Three tasks selected, drag one → all three move to target in order
□ Phase + Action selected, drag → only Phase moves
□ npm run typecheck passes
```

**Progress log:** `docs/progress/sessions/[date]-[agent]/B9-multiselect-drag.md`
