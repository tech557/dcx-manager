# Sprint B8 — View Context Island

**Preconditions:** B-FIL complete
**Rollback boundary:** Revert only this sprint's changed files if typecheck fails

> See `docs/plans/active/builder-refactor/agent-execution-guide.md` for the task format and agent execution notes applicable to all tasks.


## B8.1 — View Context task list UI ✅ BLD-VCX-001 BLD-VCX-002

**Requirements:** BLD-VCX-001, BLD-VCX-002, BLD-FIL-001  
**Precondition:** B-FIL complete — ViewHelperIsland shell is empty of file preview  
**Files to inspect:** `src/builder/islands/ViewHelperIsland/ViewHelperIsland.tsx` (after B-FIL.2), `src/builder/islands/ViewHelperIsland/useViewHelperScrollers.ts`, `src/types/builder-node.types.ts`  
**Files to change:** `src/builder/islands/ViewHelperIsland/ViewHelperIsland.tsx`, `src/builder/islands/ViewHelperIsland/ViewContextTaskList.tsx` (CREATE), `src/builder/islands/ViewHelperIsland/ViewContextTaskItem.tsx` (CREATE)

**Required behaviour:**
- Replace placeholder text with ViewContextTaskList
- Show ALL Tasks — unassigned (`date.mode === 'unset'`) as active/draggable; assigned (date set) as visible but DISABLED ✅ (BLD-VCX-002 — do not filter assigned tasks out)
- Group by Phase → Action
- Each item: channel pill, task name, action name, phase name
- Unassigned: draggable with payload `{ id: task.id, kind: 'task', fromViewContext: true }`
- Assigned: greyed, not draggable
- Empty state: "All tasks have communication dates assigned."
- Zero file preview in this component ✅ BLD-FIL-001

## B8.2 — Drag-to-Day task assignment ✅ BLD-VCX-003

**Files to inspect:** `src/builder/stage/views/DayGridCard.tsx`, `src/builder/stage/views/useDayGridDrag.ts`, `src/actions/task.actions.ts`  
**Files to change:** `src/builder/stage/views/useDayGridDrag.ts`, `src/actions/task.actions.ts`

**Required behaviour:** When `fromViewContext: true` in drop payload: call `actions.updateTask({ taskId: payload.id, changes: { date: { mode: 'fixed', date: dayDateString } } })`. Task stays in its Phase/Action — only date changes. After update, task's `date.mode` is no longer 'unset' → appears disabled in View Context.

**Acceptance criteria:**
```
□ Unassigned tasks shown as active; assigned tasks shown as visible+disabled ✅
□ Dragging unassigned task to Day assigns date
□ Task becomes disabled in View Context after assignment
□ Zero file preview in ViewHelperIsland ✅ BLD-FIL-001
□ npm run typecheck passes
```

**Progress log:** `docs/progress/sessions/[date]-[agent]/B8-view-context.md`
