# Sprint B1 — Builder Loading Shell

**Status: ✅ COMPLETED (2026-06-25 by Gemini Agent 3)**

**Preconditions:** B0 complete
**Rollback boundary:** Revert only this sprint's changed files if typecheck fails

> See `docs/plans/active/builder-refactor/agent-execution-guide.md` for the task format and agent execution notes applicable to all tasks.


## B1.1 — Create BuilderLoadingShell and replace text placeholder

**Requirements:** BLD-LOD-001  
**Files to inspect:** `src/builder/BuilderPage.tsx`  
**Files to change:** `src/builder/BuilderLoadingShell.tsx` (CREATE), `src/builder/BuilderPage.tsx` (EDIT)

**Required behaviour:** Replace the `<section className="placeholder-screen">` with `<BuilderLoadingShell />`. BuilderLoadingShell renders the three-row builder layout as skeletons — same padding, gaps, row heights as the live workspace. Row 1: glass pill skeleton (~64px). Row 2: left narrow skeleton (56px), three phase-width skeleton columns in centre, right narrow (56px). Row 3: three island pill skeletons. Shimmer animation via CSS only. Error state: dark glass container with message and retry button.

**Preserve-semantic:** No island, card, or stage component touched.

**Acceptance criteria:**
```
☑ BuilderLoadingShell.tsx ≤ 80 lines
☑ No "Preparing workspace" text visible anywhere
☑ Skeleton row heights match live workspace exactly
☑ Shimmer is CSS-only
☑ Error state has retry button
☑ No layout shift on data arrival
☑ npm run typecheck passes
```

**Agent Execution Notes:**  
*With terminal:* Run typecheck. Navigate to /builder/v-1 with DevTools → Network → Slow 3G. Confirm skeleton renders before data.  
*Without terminal:* Return complete BuilderLoadingShell.tsx and BuilderPage.tsx. Manual check: "Throttle network, load builder, confirm three-row skeleton appears before content."

**Progress log:** `docs/progress/sessions/[date]-[agent]/B1-loading-shell.md`
