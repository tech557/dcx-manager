# Sprint B2 — Stage Sizing

**Status: ✅ COMPLETED (2026-06-25 by Gemini Agent 3)**

**Preconditions:** B0 complete
**Rollback boundary:** Revert only this sprint's changed files if typecheck fails

> See `docs/plans/active/builder-refactor/agent-execution-guide.md` for the task format and agent execution notes applicable to all tasks.


## B2.1 — Remove fixed max-width from StageCore

**Requirements:** BLD-STG-003  
**Files to inspect:** `src/builder/stage/StageCore.tsx`  
**Files to change:** `src/builder/stage/StageCore.tsx`

**Required behaviour:** Remove `max-w-[1280px]` from the canvas wrapper div. Replace with `w-full h-full`. The outer flex container already bounds width via the three-column Row 2 layout. Verify `max-h` is handled by flex row height, not a fixed pixel value. Keep `pointer-events-auto`, `overflow-hidden`, `transition-all`.

**Preserve-semantic:** Stage rendering, edge navigation, all views unchanged.

**Acceptance criteria:**
```
☑ At 1440px screen with editor closed: stage fills available horizontal space
☑ Phase cards not clipped on right edge at default density
☑ Stage does not overflow page vertically
☑ Editor open: stage narrows correctly
☑ npm run typecheck passes
```

**Agent Execution Notes:**  
*With terminal:* Run typecheck. Open builder at 1440px, check stage fills available width.  
*Without terminal:* Return StageCore.tsx. Manual check: "Open builder at full width. Confirm cards are not cut off."

**Progress log:** `docs/progress/sessions/[date]-[agent]/B2-stage-sizing.md`
