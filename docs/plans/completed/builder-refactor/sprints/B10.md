# Sprint B10 — View Transition Animation

**Preconditions:** B2 complete
**Rollback boundary:** Revert only this sprint's changed files if typecheck fails

> See `docs/plans/active/builder-refactor/agent-execution-guide.md` for the task format and agent execution notes applicable to all tasks.


## B10.1 — View transition animation

**Requirements:** BLD-ANM-001  
**Files to inspect:** `src/builder/stage/StageCore.tsx`, `src/ui/motion/effects.registry.ts`  
**Files to change:** `src/builder/stage/StageCore.tsx`, `src/ui/motion/effects.registry.ts`

**Required behaviour:** Wrap `<Renderer>` in `<AnimatePresence mode="wait">` with `key={view}`. Add `viewTransitionIn` and `viewTransitionOut` to effects registry. Kanban→Timeline: left exit + right enter. Timeline→Kanban: reverse. Duration: 220ms ✅ BLD-MOT-001 / OD-006. Use motion (Framer) only — never gsap for component animation.

**Acceptance criteria:**
```
□ View switch animates directionally
□ Selection state preserved after transition
□ npm run typecheck passes
```

**Progress log:** `docs/progress/sessions/[date]-[agent]/B10-view-transition.md`
