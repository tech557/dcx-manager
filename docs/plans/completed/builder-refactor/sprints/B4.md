# Sprint B4 — Card Reveal and Temp Selection

**Preconditions:** B-CRD complete
**Rollback boundary:** Revert only this sprint's changed files if typecheck fails

> See `docs/plans/active/builder-refactor/agent-execution-guide.md` for the task format and agent execution notes applicable to all tasks.


## B4.1 — Newly created card reveal + temporary selection

**Requirements:** BLD-CRD-001  
**Files to inspect:** `src/actions/phase.actions.ts`, `src/actions/action.actions.ts`, `src/actions/task.actions.ts`, `src/builder/stage/StageProvider.tsx`  
**Files to change:** action files + StageProvider.tsx

**Required behaviour:** After createPhase/createAction/createTask: add new id to `selectedNodeIds`. After 1500ms: remove from `selectedNodeIds`. The `isJustCreated` highlight in CardShell already fires for 850ms from mount. This task adds the selection layer which lasts longer. For multiple created items: add all ids, reveal sequentially.

**Preserve-semantic:** Action boundary unchanged. No new state in card templates.

**Acceptance criteria:**
```
□ Creating Phase/Action/Task: new card briefly selected + highlight
□ After ~1.5s: auto-deselects
□ npm run typecheck passes
```

**Progress log:** `docs/progress/sessions/[date]-[agent]/B4-card-reveal.md`
