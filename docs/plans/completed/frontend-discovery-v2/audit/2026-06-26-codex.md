---
audit-of: frontend-discovery-v2
auditor: codex
date: 2026-06-26
verdict: READY
blocking-issues: 0
advisory-issues: 2
resolution-round: 3
---

# Plan Audit: frontend-discovery-v2

## Verdict

READY

**Reason:** Claude fixed the real frontend blockers. FE2-R1 now treats `components.json` as a dictionary keyed by component name and derives consumer counts from `component-usages.json`. FE2-R3 now uses `definedIn`, not a nonexistent `file` field, and also derives consumer counts from `component-usages.json`. The synthesis paths are now repo-relative.

## Blocking issues

None.

## Advisory issues

| # | Sprint | Issue | Evidence | Suggested fix |
|---|---|---|---|---|
| 1 | FE2-R1 | Step 1 still checks `code-index/code-index-metadata.json`, which is not present in the current code-index folder. The sprint falls back to "metadata file not found - run: npm run generate:code-index", so execution is not blocked. | Current index files include `components.json` and `component-usages.json`; no metadata file is present. `verify-tooling-state.sh` reports the index is stale. | Either make `generate:code-index` write the expected metadata file or rely on `verify-tooling-state.sh` as the freshness source. |
| 2 | FE2-R1 / FE2-R3 | Code index is stale, so any executor must regenerate before relying on inventory counts. FE2-R1 already instructs this when stale. | Current session: `code_index_stale: true`, age 1463 minutes. | Run `npm run generate:code-index` before FE2-R1 Step 4/5 and before FE2-R3 if R1/R2 are not freshly generated. |

## Verification probes

- Current `components.json` shape is a dictionary. Its component records use `definedIn`, `exportType`, `props`, and `childComponentsUsed`.
- Current `component-usages.json` is a list with `component`, `usedIn`, `resolvedPath`, `line`, `origin`, and `propsPassed`.
- FE2-R1's revised Step 5 matches this shape: `components = list(data.keys())`, with `Counter(u['component'] for u in usages)`.
- FE2-R3's revised Step 3 matches this shape: it iterates `for name, info in components.items()`, reads `info.get('definedIn', '')`, and uses usage-derived consumer counts.

## Prior art compliance

The plan preserves the required prior-art chain:

- FE2-R1 reads expired `src-structure-audit` and expired FE-R1.
- FE2-R2 reads expired FE-R2 and the `src-structure-refactor` README.
- FE2-R3 reads expired FE-R3 and the `src-structure-refactor` README.

The previous concern about `src/components/forms/` is covered by FE2-R1 Step 6.

## Gate coverage summary

| Sprint | typecheck | lint | validate:architecture | test | browser | Notes |
|---|---|---|---|---|---|---|
| FE2-R1 | N/A | N/A | Audit subject | N/A | N/A | Discovery sprint, no source changes. |
| FE2-R2 | N/A | Audit subject | N/A | N/A | N/A | Discovery sprint, no source changes. |
| FE2-R3 | N/A | N/A | N/A | N/A | N/A | Discovery sprint, no source changes. |

Current session evidence: repository version `v0.3.2`; active plans `[]`; MCP operational `eslint`; MCP awaiting setup `storybook`, `shadcn`, `semgrep`, `sonarqube`; code index stale by 1463 minutes.

## Handoff quality

The revised frontend plan is now executable and should not produce the false all-orphan/all-unknown output that the earlier version would have produced. My signoff is conditional only on regenerating the stale index before collecting final counts, which the sprint already instructs.

## Ready checklist

- [x] All blocking issues resolved
- [x] Prior art findings incorporated
- [x] Every sprint has executor named
- [x] Every code-modifying sprint has gate coverage
- [x] Session start steps present in each sprint
