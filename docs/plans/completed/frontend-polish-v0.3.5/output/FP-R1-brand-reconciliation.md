---
sprint: FP-R1
agent: Claude (claude-sonnet-4-6, Anthropic)
date: 2026-06-28
impeccable-mode: brand-audit (identity-preservation — committed brand colors found in src/brand/)
brandbook-gate: RESOLVED BY CODEX FOLLOW-UP — see output/brandbook-values.md and output-review/2026-06-28-codex-FP-R1-pdf-check.md.
---

# FP-R1 — Brand Reconciliation: design-system ↔ brandbook

## §0 — Brandbook gate status

**RESOLVED BY CODEX FOLLOW-UP — brandbook values extracted.**

Codex rechecked the PDF gate on 2026-06-28 using the bundled runtime. The PDF is not fully image-only
in this environment: `pypdf` extracts text from the brandbook, including the color pages, and Poppler
renders pages 11-14. Use `output/brandbook-values.md` as the extracted brandbook values source.

The original Claude note below is retained as historical context for why D-08 was opened, but the
blocking gate is superseded by `output-review/2026-06-28-codex-FP-R1-pdf-check.md`.

**Original blocked note (superseded):**

The file at `brandbook.pdf` (4.1 MB, 57 pages) was generated by Adobe Illustrator as a fully
rasterised export: 49 JPEG-embedded page images, zero PDF text blocks, zero font render calls.
No Python library (`pypdf`, `pdfplumber`), CLI tool (`pdftotext`, `pdftoppm`), or metadata parser
can extract color/typography/spacing values from JPEG-compressed raster images without a PDF
renderer + OCR stack (poppler + tesseract), neither of which is installed in this environment.

**Partial audit performed:** All `src/brand/` tokens audited against:
- `PRODUCT.md` brand personality and accessibility constraints
- `impeccable` product-register rules (identity-preservation mode: committed brand colors kept)
- FP-R0 live-builder findings (token drift, pure-white offenders, contrast failures)
- XMP metadata embedded in `brandbook.pdf` (fonts list extractable from binary)

**Brandbook-sourced columns** are now supplemented by `output/brandbook-values.md`. D-08 is resolved; any remaining table cells that say `see brandbook-values.md` should be read through that file rather than treated as blocked.

**One new finding from XMP metadata:** `29LT Zarid Slab Black` (OTF) is embedded in the
brandbook alongside Gilroy — this is a slab-serif display font not present in `src/brand/`.
It is flagged as a potential brand-mandated display typeface in §3.

---

## §1 — Accent / interactive color tokens

| Token | Dark value | Light value | Brandbook value | Verdict | Contrast note |
|---|---|---|---|---|---|
| `--theme-accent` | `#75E2FF` | `#75E2FF` | **`#75E2FF` ✅ CONFIRMED** — "Blue: C50 M0 Y5 K0 — evokes tranquility, freshness, belonging" | **keep (identity)** — exact match to brandbook primary blue | Fails 4.5:1 on light surfaces (`#FFFFFF` bg → ~2.8:1). Passes on dark bg `#0a0a0d` → ~9.4:1. **Never use as text on light bg.** |
| `--theme-accent-variant` | `#55c2df` | `#55c2df` | N/A — product-derived (darker variant of brand blue, not in brandbook) | keep — secondary accent | Slightly better on light (~3.5:1) but still fails WCAG AA for small text |
| `--theme-accent-deep` | `#006080` | `#006080` | N/A — product-derived (pressed/deep state, not in brandbook) | keep — only accent-family token safe as text on light bg (≈4.7:1) | Passes 4.5:1 on near-white ✅ |
| `--theme-accent-subtle` | `rgba(117,226,255,0.08)` | `rgba(117,226,255,0.08)` | N/A — product-derived alpha scale | keep — background tint | Not used for text |
| `--theme-accent-soft` | `rgba(117,226,255,0.12)` | same | N/A — product-derived | keep | Not used for text |
| `--theme-selected-glow` | `rgba(117,226,255,0.15)` | same | N/A — product-derived | keep | Glow only |
| `--theme-accent-bg` | `rgba(117,226,255,0.2)` | same | N/A — product-derived | keep | Background only |
| `--theme-accent-medium` | `rgba(117,226,255,0.3)` | same | N/A — product-derived | keep | Border/divider |
| `--theme-accent-strong` | `rgba(117,226,255,0.5)` | same | N/A — product-derived | keep | Border emphasis |
| `--theme-component-focus-accent` | `rgb(117,226,255)` | same | N/A — product-derived | keep — keyboard focus ring | Against dark glass surface: passes |

---

## §2 — Semantic status tokens

| Token | Dark value | Light value | Brandbook value | Verdict | Contrast note |
|---|---|---|---|---|---|
| `--theme-error` | `#FF7575` | `#FF7575` | N/A — product semantic (no error color in brandbook) | keep — semantic color | Fails 4.5:1 on white (≈3.1:1). Do not use as text on light bg. |
| `--theme-error-alt` | `#FF6464` | same | N/A — product-derived | keep | Same contrast concern |
| `--theme-error-bg` | `rgba(255,117,117,0.12)` | same | N/A — product-derived | keep | Background only |
| `--theme-error-deep-bg` | `#241113` (dark) | `rgba(255,117,117,0.16)` (light) | N/A — product-derived | keep | Deep error surface |
| `--theme-error-bg-alt` | `rgba(255,100,100,0.2)` | same | N/A — product-derived | keep | |
| `--theme-warning` | `#F8C458` | `#F8C458` | N/A — product semantic. Note: brandbook secondary Mellow Yellow `#FFDFA0` is in the same warm-yellow family but is much lighter and scoped to graphs/static use. | keep — no brandbook conflict | Fails 4.5:1 on white (≈2.5:1). Warning text must use darker sibling or dark bg. |
| `--theme-warning-bg` | `rgba(248,196,88,0.2)` | same | N/A — product-derived | keep | Background only |
| `--theme-warning-glow` | `rgba(245,158,11,0.35)` | same | N/A — product-derived | keep | |
| `--theme-success` | `rgba(52,211,153,1)` = `#34D399` | same | N/A — product semantic. Note: brandbook secondary Magic Mint `#D0EFDA` is in the mint-green family but is a very light tint; scoped to static/graphs. | keep — no brandbook conflict | Fails 4.5:1 on white (≈2.9:1). Use as icon/indicator only on light bg, not text. |
| `--theme-success-bg` | `rgba(16,185,129,0.2)` | same | N/A — product-derived | keep | Background only |
| `--theme-success-glow` | `rgba(16,185,129,0.35)` | same | N/A — product-derived | keep | |
| `--theme-info` | `rgba(56,189,248,0.7)` | same | N/A — product semantic. Note: brandbook secondary Blue Purple `#ADB1E8` is blue but muted/lavender; scoped to static/graphs. | **change-token candidate** — opacity at 0.7 is ambiguous; solidify to opaque for text use | Opaque `#38BDF8` on white ≈ 2.5:1 (fails); use as indicator/icon only |
| `--theme-info-bg` | `rgba(56,189,248,0.1)` | same | N/A — product-derived | keep | Background only |

---

## §3 — Surface / background tokens

| Token | Dark value | Light value | Verdict | Contrast note |
|---|---|---|---|---|
| `--theme-surface-deep` | `#050506` | `#FAF9F6` | keep dark / **change-token light** — `#FAF9F6` is warm near-white acceptable. Brandbook Grey `#E3E4E5` (C0 M0 Y0 K10) is the closest brand neutral; `#FAF9F6` is lighter/warmer. After surface-void correction, consider aligning light surfaces toward `#E3E4E5` territory if PO confirms. | Light: warm-tinted neutral, not pure white. Acceptable provisionally. |
| `--theme-surface-void` | `#0a0a0d` | `#FFFFFF` | keep dark / **change-token light — CRITICAL** | `#FFFFFF` is pure white — violates PRODUCT.md "no pure white tokens" and impeccable identity rules. Must change to a near-white with brand-hue tint (see §6 recommendations). |
| `--theme-surface-deep-alt` | `#0e0f12` | `#F7F7F8` | keep dark / keep light — cool near-white acceptable | Light: `#F7F7F8` is near-white, no chroma. Could add minimal blue-cool tint to align with accent hue. |
| `--theme-surface-dark` | `#161617` | `#E9ECEF` | keep both | Cool-light surface; acceptable |
| `--theme-dropdown-bg` | `#121212` | `#FFFFFF` | keep dark / **change-token light — CRITICAL** | `#FFFFFF` pure white. Same violation as `surface-void`. |
| `--theme-surface-alternate` | `#0c0d0f` | `#F1F3F5` | keep both | Acceptable neutral |
| `--theme-glass-bg` | `rgba(13,13,14,0.88)` | `rgba(255,255,255,0.85)` | keep dark / **change-token light** — glass bg at 85% on pure-white-resolved surface will render as near-white glass | On a corrected surface-void, this may be acceptable. Re-evaluate after surface-void fix. |

---

## §4 — Text tokens

| Token | Dark value | Light value | Verdict | Contrast note |
|---|---|---|---|---|
| `--theme-text-primary` | `#F7F7F8` | `#151516` | keep both | Dark: #F7F7F8 on #0a0a0d → ≈17:1 ✅. Light: #151516 on #FAF9F6 → ≈15:1 ✅ |
| `--theme-text-muted` | `rgba(247,247,248,0.72)` | `rgba(21,21,22,0.68)` | **check light** | Light: resolves to ~#656465 on #FAF9F6 → ≈5.5:1 ✅ (marginal — check on any tinted bg) |
| `--theme-text-secondary` | `""` (empty) | `""` (empty) | **add token — CRITICAL** | Both themes have an empty string value. This token is referenced but never defined. Must be set. Recommended: dark = `rgba(247,247,248,0.78)`, light = `rgba(21,21,22,0.78)` |
| `--theme-muted` | `rgba(247,247,248,0.4)` | `rgba(21,21,22,0.4)` | **change-token** — at 40% opacity this is decorative-only territory; not safe for small text | Dark on dark: ≈3.6:1 on #0a0a0d (fails 4.5:1 for small text). Use only for non-text elements (dividers, disabled icons). |

---

## §5 — Component surface tokens

These tokens drive the glass island visual language. All are dark-theme-anchored (dark glass over
dark canvas). The light theme uses the same values, creating a mismatch where glass islands appear
dark-on-light — confirmed in FP-R0 §5 findings.

| Token | Value | Verdict |
|---|---|---|
| `--theme-component-surface-nav` | `rgba(13,13,14,0.72)` | keep — nav glass dark, expected |
| `--theme-component-surface-pill` | `rgba(13,13,14,0.86)` | keep |
| `--theme-component-surface-header` | `rgba(13,13,14,0.84)` | keep |
| `--theme-component-surface-editor` | `rgba(13,13,14,0.92)` | keep |
| `--theme-component-surface-glass-dark` | `rgba(13,13,14,0.45)` | keep |
| `--theme-component-surface-deep` | `rgb(13,13,14)` | keep |
| `--theme-component-surface-popup` | `rgb(21,21,22)` | keep |
| **Light theme equivalent** | (none — same dark values used) | **PO decision required** — see D-05 / D-02 in register. Should light theme island surfaces be a light-glass variant or keep dark-glass-on-light? |

---

## §6 — Component text tokens

All component text tokens use `#F7F7F8` (near-white) as the base — correct for dark glass islands.
These do not need light-theme variants IF islands remain dark-glass in light mode (per §5 PO decision).

| Token | Value | Verdict |
|---|---|---|
| `--theme-component-text-primary` | `rgb(247,247,248)` | keep |
| `--theme-component-text-muted` | `rgba(247,247,248,0.72)` | keep — ≈7.6:1 on deep surface ✅ |
| `--theme-component-text-subtle` | `rgba(247,247,248,0.62)` | keep — ≈6.1:1 ✅ |
| `--theme-component-text-soft` | `rgba(247,247,248,0.66)` | keep — ≈6.6:1 ✅ |
| `--theme-component-text-secondary` | `rgba(247,247,248,0.78)` | keep |
| `--theme-component-text-disabled` | `rgba(247,247,248,0.56)` | acceptable for disabled state (not required to pass 4.5:1) |
| `--theme-component-text-faint` | `rgba(247,247,248,0.45)` | acceptable for decorative/labels only |

---

## §7 — Border tokens

| Token | Value | Verdict |
|---|---|---|
| `--theme-border-subtle` | dark: `rgba(255,255,255,0.08)` / light: `rgba(0,0,0,0.08)` | keep — theme-aware |
| `--theme-divider` | dark: `rgba(255,255,255,0.1)` / light: `rgba(0,0,0,0.08)` | keep |
| `--theme-component-border` | `rgba(255,255,255,0.08)` | keep |
| `--theme-component-border-muted` | `rgba(255,255,255,0.06)` | keep |
| `--theme-component-border-soft` | `rgba(255,255,255,0.07)` | keep |
| `--theme-component-border-strong` | `rgba(255,255,255,0.1)` | keep |
| `--theme-component-border-faint` | `rgba(255,255,255,0.05)` | keep |
| `--theme-component-border-light` | `rgba(0,0,0,0.08)` | keep — light-context border |

---

## §8 — Shadow tokens

| Token | Value | Verdict |
|---|---|---|
| `--theme-component-shadow-soft` | `rgba(0,0,0,0.2)` | keep |
| `--theme-component-shadow-medium` | `rgba(0,0,0,0.3)` | keep |
| `--theme-component-shadow-strong` | `rgba(0,0,0,0.35)` | keep |
| `--theme-component-shadow-glow` | `rgba(0,0,0,0.4)` | keep |
| `--theme-component-shadow-light` | `rgba(0,0,0,0.04)` | keep |
| `shadowStyleTokens.island` (ts) | `0 12px 40px rgba(0,0,0,0.2)` | keep |
| `shadowStyleTokens.card` (ts) | `0 8px 32px rgba(0,0,0,0.3)` | keep |
| `shadowStyleTokens.overlay` (ts) | `0 20px 50px rgba(0,0,0,0.3)` | keep |
| `shadowStyleTokens.glow` (ts) | `0 0 15px var(--theme-selected-glow)` | keep |

---

## §9 — Radius tokens

| Token | Value | Verdict |
|---|---|---|
| `--radius` | `0.625rem` (10px) | keep — base radius; drives scale below |
| `--radius-sm` | `calc(var(--radius) * 0.6)` = 6px | keep |
| `--radius-md` | `calc(var(--radius) * 0.8)` = 8px | keep |
| `--radius-lg` | `var(--radius)` = 10px | **bug — `--radius-lg: ""` was observed empty in FP-R0 eval**. In `theme.css` it resolves correctly as `var(--radius)`. The empty value seen in eval was likely a Tailwind v4 resolution issue. Verify via computed styles in dev — if empty at runtime, hardcode to `0.625rem`. |
| `--radius-xl` | `calc(var(--radius) * 1.4)` = 14px | keep |
| `--radius-2xl` | `calc(var(--radius) * 1.8)` = 18px | keep |
| `--radius-3xl` | `calc(var(--radius) * 2.2)` = 22px | keep |
| `--radius-4xl` | `calc(var(--radius) * 2.6)` = 26px | keep |

Impeccable rule: cards top out at 12–16px. `--radius-lg` at 10px and `--radius-xl` at 14px are both
within range. No over-rounding violation.

---

## §10 — Typography tokens

### Font family

| System | Current | Brandbook | Verdict |
|---|---|---|---|
| Primary / UI | Gilroy (100–950, all weights + italic) | Gilroy ✅ (confirmed via XMP) | **keep** — identity confirmed |
| Display / heading | Gilroy (same family) | **29LT Zarid Slab Black** found in brandbook XMP — NOT in design system | **see brandbook-values.md + add-token candidate** — PO must confirm if 29LT Zarid Slab is the brand display typeface for headings/logo, or if it only appears in the brandbook cover itself |
| Fallback | system-ui (via Geist Variable in theme.css `@theme inline`) | — | **change-token** — `theme.css` line 26 overrides `--font-sans` with `'Geist Variable'`; the intended brand sans is Gilroy. This may be a shadcn/ui scaffold remnant. Verify and remove if Gilroy is authoritative. |

### Font scale

All sizes use px-level custom properties (`--text-4xs` through `--text-base`), not fluid clamp().
This is correct for a product UI (fixed rem scale per impeccable product register). Scale runs
7px–15px — a micro-dense scale for canvas labels and card metadata. Verdict: **keep all** —
this is a deliberate density choice, not drift.

| Token | Value | Note |
|---|---|---|
| `--text-4xs` | 0.4375rem (7px) | Extreme density — acceptable for date badges |
| `--text-3xs` | 0.5rem (8px) | |
| `--text-3xs-plus` | 0.53125rem (8.5px) | |
| `--text-2xs` | 0.5625rem (9px) | |
| `--text-2xs-plus` | 0.59375rem (9.5px) | |
| `--text-xs` | 0.625rem (10px) | |
| `--text-xs-plus` | 0.65625rem (10.5px) | |
| `--text-sm` | 0.6875rem (11px) | |
| `--text-md` | 0.75rem (12px) | Default label size |
| `--text-md-plus` | 0.8125rem (13px) | |
| `--text-base` | 0.9375rem (15px) | Largest defined size — missing 16px+ for body prose |

**Missing:** No `--text-lg` (16px) or larger. `components.css` uses `1.05rem` and `clamp(2.25rem,8vw,5rem)` as one-off values — these should be tokenized.

---

## §11 — Motion tokens (tokens.ts)

| Token | Value | Verdict |
|---|---|---|
| `springTokens.island` | `{type:'spring', stiffness:180, damping:24, mass:0.95}` | keep — gentle island expand |
| `springTokens.card` | `{type:'spring', stiffness:200, damping:20}` | keep |
| `springTokens.gentle` | `{duration:0.5, ease:[0.16,1,0.3,1]}` | keep — expo-out curve, per impeccable rules |

**Missing tokens:**
- No `reducedMotion` token or flag — the `prefers-reduced-motion` branch is enforced by each component individually (if at all). A shared `reducedMotion` constant or CSS variable would make the system auditable. Add-token candidate.
- Blur tokens (`heavy`, `mid`, `light`) are Tailwind class names, not CSS custom properties. Not portable to CSS context. Change-token candidate if non-Tailwind components need blur.

---

## §12 — Shadcn / Tailwind scaffold tokens (light theme `:root`)

The light `:root` in `tokens.css` contains shadcn/ui default OKLCH tokens (`--background`,
`--foreground`, `--card`, `--primary`, etc.). These are zero-chroma grays with no brand hue.

| Finding | Detail | Verdict |
|---|---|---|
| `--background: oklch(1 0 0)` | Pure white in OKLCH | **change-token** — replace with brand-tinted near-white once surface-void is corrected |
| `--foreground: oklch(0.145 0 0)` | Near-black, zero chroma | **change-token** — tint toward brand hue (cyan-cool) |
| `--accent: oklch(0.97 0 0)` | Near-white, no brand color | **change-token** — should alias to `--theme-accent` |
| `--destructive: oklch(0.577 0.245 27.325)` | Red, no brand relationship | keep — semantic only |
| All chart tokens | Gray grays | **change-token** — if charts exist, they need brand-aware palette |

These shadcn tokens are **not currently used by the builder** (the builder uses `--theme-*` exclusively). However, any future shadcn/ui component import will pull these values. Aligning them now prevents future drift.

---

## §12b — Brandbook primary neutral: Grey `#E3E4E5`

The brandbook defines a primary neutral (Grey, C0 M0 Y0 K10, `#E3E4E5`) that is **not in the
design system**. This is a light cool-grey — lighter than `--theme-surface-dark: #E9ECEF` in
light theme and notably different from the warm-tinted `#FAF9F6` surface-deep.

| Brandbook | `#E3E4E5` | Closest current token | `--theme-surface-dark: #E9ECEF` (light) |
|---|---|---|---|
| CMYK | C0 M0 Y0 K10 | Delta | 4 steps warmer; similar L* |
| Usage in brandbook | Primary neutral — backgrounds, type containers | Proposed app usage | Light-theme neutral panel background (add-token candidate, not urgent) |

Verdict: **add-token candidate** — low priority. After surface-void correction, evaluate whether
`--theme-surface-dark` in light theme should be adjusted to `#E3E4E5` or a near-equivalent
OKLCH value to align with the brand neutral.

---

## §15 — Brandbook secondary palette (not in design system)

The brandbook defines 4 secondary color families (12 tokens: base + shade + tint each).
These are scoped to **"rare usage / graphs / limited coloring / use on white or grey backgrounds
only."** None are present in `src/brand/`.

| Name | Base | Shade | Tint | Current design system token | Verdict |
|---|---|---|---|---|---|
| Mellow Yellow | `#FFDFA0` | `#EBCD93` | `#FFF7E6` | None | **add-token candidate** — data-viz / chart accent 1. Warm yellow family; do not confuse with `--theme-warning` (#F8C458, more saturated amber). |
| Vivid Tangerine | `#FFA089` | `#EB937E` | `#FFE0D9` | None | **add-token candidate** — data-viz / chart accent 2. Coral-salmon; no conflict with existing semantic colors. |
| Magic Mint | `#D0EFDA` | `#C1DECA` | `#F0F8F1` | None | **add-token candidate** — data-viz / chart accent 3. Very light mint; do not use as a success indicator (too low contrast, `#D0EFDA` on white ≈ 1.3:1). |
| Blue Purple | `#ADB1E8` | `#979BCD` | `#EBECFA` | None | **add-token candidate** — data-viz / chart accent 4. Muted lavender-blue; no conflict with `--theme-accent: #75E2FF`. |

**Important constraints from brandbook:**
- Use secondary colors on white or grey backgrounds only (not on dark surfaces or over accent)
- Rare / limited use — not for primary UI chrome
- Intended for static media contexts (email, social, infographics); web data-viz is the most
  reasonable app extension

**Recommendation:** Do not add these to `src/brand/tokens.css` until a specific data-visualization
or charting use case is confirmed. When that use case arrives, add as a `--theme-chart-*` token
group. Flag for FP-R5 to include in implementation sprint scope if charting is planned.

---

## §13 — Correction priority list

**CRITICAL (block light-theme shipping):**
1. `--theme-surface-void` light: `#FFFFFF` → change to near-white with cool tint (e.g. `oklch(0.99 0.004 220)`)
2. `--theme-dropdown-bg` light: `#FFFFFF` → same correction
3. `--theme-text-secondary`: empty string in both themes → add values (see §4)

**HIGH (fix before FP-R5 implementation):**
4. Geist Variable fallback in `theme.css` overriding Gilroy — remove or make conditional
5. `--theme-info` at 0.7 opacity — solidify if used as text color
6. `--radius-lg` empty at runtime — verify and fix if needed
7. `--theme-accent` on light bg — add a guardrail rule to brand-ui-interpretation.md

**MEDIUM:**
8. 29LT Zarid Slab as display font — PO must confirm app usage before adding to `src/brand/fonts/` (likely Arabic/static context only per brandbook)
9. Brandbook Grey `#E3E4E5` — evaluate whether `--theme-surface-dark` light value should align to this after surface-void is corrected
10. Shadcn OKLCH tokens — align `--background` and `--foreground` to brand palette; `--accent` should alias `--theme-accent`
11. Secondary palette (`#FFDFA0`, `#FFA089`, `#D0EFDA`, `#ADB1E8`) — add as `--theme-chart-*` tokens only when a data-viz use case is confirmed; skip until then

**LOW (add-token, no breakage if deferred):**
11. `--text-lg` and larger text sizes for body/display prose
12. `reducedMotion` constant / CSS variable
13. Blur tokens as CSS custom properties (not just Tailwind classes)

---

## §14 — Brandbook gate resolution (D-08)

D-08 is resolved by `output/brandbook-values.md`.

The following values were extracted:
- Primary brand colors: blue, black, grey, white.
- Secondary colors: mellow yellow, vivid tangerine, magic mint, blue purple, plus listed tints/shades.
- Typography families: Gilroy and 29LT Zarid Slab.
- Static layout type sizes: examples for email, article, webinar, and social formats.
- Motion tone: sleek and smooth; rolling ments outro for major communications.

The following remain product/PO interpretation questions, not PDF extraction blockers:
- Whether 29LT Zarid Slab belongs inside the web app font stack.
- Whether static-media typography sizes should influence any app display tokens.
- How to adapt brandbook pure black/white into the project's no-pure-black/no-pure-white UI rule.
