---
audit-of: cicd-release-governance
auditor: codex
date: 2026-07-01
verdict: READY
blocking-issues: 0
advisory-issues: 4
---

# Plan Audit: cicd-release-governance

## Verdict

READY

**Reason:** The previous activation blockers are resolved: sprint files are executable, requirement traces now cite canonical approved graph IDs, close gates are wired, and the git / first-production boundaries are explicit.

## Blocking issues (must fix before activation)

| # | Sprint | Issue | Evidence | Fix |
|---|---|---|---|---|
| None | — | — | — | — |

## Advisory issues (should fix, won't block)

| # | Sprint | Issue | Evidence | Suggested fix |
|---|---|---|---|---|
| 1 | README | One stale local paragraph still calls OD-RG-02 open even though §9.2 says all OD-RG decisions are resolved. | README §3.2 says `OD-RG-02 (open)`, while README banner and §9.2 say OD-RG-02..09 are PO-confirmed. | Change that paragraph to `OD-RG-02 (decided)` and keep the chosen reset rule. |
| 2 | RG-R5 | The Supabase fallback still talks about separate-project cost not being approved after the plan records `D-RG-ENV` as separate production project. | RG-R5 Fallbacks: `Separate-project cost not approved → default to preview branches for all non-prod`; README Decisions says production is a separate project. | Reword the fallback to say lack of Supabase access pauses live setup, but the decided target remains separate production project. |
| 3 | RG-R8 | `D-RG-VER` is still listed inside the Requirements row with requirement IDs. This is not fatal, but decisions and requirements should stay distinct for trace hygiene. | RG-R8 Requirement Trace lists `REQ-RG-PROD-004, ... D-RG-VER` and then says all are canonical graph nodes. | Move `D-RG-VER` to a separate `Decisions` row or note it in Step 0 only. |
| 4 | Graph generated view | The generated requirement index appears stale even though JSON nodes and `req:validate` are correct. | `docs/product/requirements/graph/nodes/requirement/README.md` still lists `REQ-RG-*` rows as `proposed`; JSON nodes show `governance: approved`, and `npm run req:validate` passes. | Regenerate graph views with `npm run req:generate-views` before or during activation so human readers do not see stale status. |

## Prior art compliance

The plan incorporates the relevant prior art:

- `completed/requirements-system`: the release-governance requirements were taken through the governed intake path. There are 19 canonical `REQ-RG-*` JSON nodes, `req:validate` passes with zero errors and warnings, and sprint traces now read approved / PO-locked.
- `completed/folder-structure-v2`: the plan preserves no-`src/**` discipline, uses a carry-forward contract, gives exact output paths, records no-recreate/reuse homes for new release tooling, and keeps evidence under plan output folders.

No matching expired CI/CD release-governance plan was found under `docs/plans/expired/`.

## Gate coverage summary

| Sprint | typecheck | lint | validate:architecture | test | browser | Notes |
|---|---|---|---|---|---|---|
| RG-R0a | N/A | N/A | N/A | N/A | N/A | Audit-only; pre/post `src/**` hash proof; real `sprint-doctor` and requirement gates. |
| RG-R0b | N/A | N/A | N/A | N/A | Optional | PO-owned git/GitHub setup; strict pre-first-commit secret scan; no `src/**`. |
| RG-R1 | N/A | N/A | N/A | N/A | N/A | Governance docs and index builder; index integrity gate. |
| RG-R2 | N/A | N/A | N/A | shell tests | N/A | Release registry/scripts; `verify.sh`; no `src/**`. |
| RG-R3 | CI gates | CI gates | CI gates | CI gates | YES | GitHub Actions / branch protection; fallback blocks honestly if Actions unavailable. |
| RG-R4 | N/A | N/A | N/A | promotion tests | YES | Vercel capability proof first; exact-build fallback requires explicit PO acceptance. |
| RG-R5 | N/A | N/A | N/A | migration-gate test | N/A | Supabase env separation; access fallback is explicit BLOCKED. |
| RG-R6 | N/A | N/A | N/A | round-trip test | N/A | ClickUp/GAS mirror; registry remains canonical. |
| RG-R7 | CI gates | CI gates | CI gates | CI gates | YES | Dogfood uses dedicated fixtures, not product `src/**`. |
| RG-R8 | N/A | N/A | N/A | promotion gate | YES | One-time `v0.3.5.0` first-production bootstrap; PO approval required before alias move. |

Current audit evidence:

- `npm run req:validate` → pass `true`, 0 errors, 0 warnings.
- `find docs/product/requirements/graph/nodes/requirement -maxdepth 1 -name 'REQ-RG-*.json' | wc -l` → 19.
- All sprint Requirement Trace rows now say approved / canonical / PO-locked.
- `scripts/agent/sprint-doctor.sh`, `scripts/build-log-index.sh`, and `scripts/verify.sh` exist.

## Handoff quality

Handoff quality is activation-ready. Each sprint has a named executor, dependency chain, allowed and forbidden writes, Step 0 environment/carry-forward read, exact output path, acceptance criteria, gates, fallbacks, and a carry-forward close step. RG-R0b properly isolates the PO-owned git setup and includes a pre-first-commit secret scan, which is the right shape for the first irreversible repository action.

The plan also handles the unique first-production request cleanly: RG-R8 treats `v0.3.5.0` as a one-time manual bootstrap and then hands future releases back to the normal promotion rules.

## Ready checklist

- [x] All blocking issues resolved
- [x] Prior art findings incorporated
- [x] Every sprint has executor named
- [x] Every code/tooling-modifying sprint has gate coverage
- [x] Session start steps present in each sprint
- [x] Carry-forward contract present; every sprint reads it and updates it
- [x] Tool-dependent criteria have a documented fallback
- [x] Requirement traces cite canonical approved graph IDs
- [x] Git setup boundary reflects PO approval and forbids `src/**`
- [x] First-production bootstrap is explicitly modeled
