## Version boundary — archive v0.3.4 sessions, ready project for the new plan
Agent: Claude
Model: claude-opus-4-8
Provider: Anthropic
Date: 2026-06-28
Status: Completed

Intent: At the end of v0.3.4 (folder-structure-v2 P1–P6 complete and archived to completed/), archive all existing session logs (preserved) and reset the project for the next plan.
Trigger: user request — "this marks the end of version 0.3.4 ... archive all existing sessions keeping the log and keep the project ready for the new plan."
Requirements covered: N/A — version-boundary maintenance; no product src changed.

## Pre-state verified
- `folder-structure-v2` already in `docs/plans/completed/` (P6 plan-close moved it). `docs/plans/active/` had no plan dirs (only README.md + a stray `.DS_Store`).
- 13 session folders / 151 `.md` logs in `docs/progress/sessions/`.

## Actions
1. Ran `bash scripts/archive-version.sh v0.3.4 v0.4.0` — copied `docs/progress/sessions/*` → `docs/archive/sessions/v0.3.4/` and appended the "v0.4.0 Archive" entry to `docs/archive/README.md`. (The script does NOT change the version number.)
2. Copied `docs/progress/index.csv` → `docs/archive/sessions/v0.3.4/index.csv` (the script doesn't archive the index).
3. Verified archive completeness: **13 folders + 151 logs + index.csv** in `docs/archive/sessions/v0.3.4/` (matches live exactly) before removing anything.
4. Cleared `docs/progress/sessions/*` (logs preserved in archive).
5. Reset `docs/progress/index.csv` to header-only (1 line) for the new cycle.
6. Removed stray `docs/plans/active/.DS_Store`.

## Ready-for-new-plan state
- `docs/plans/active/`: clean (README.md index only — no plans).
- `docs/plans/completed/`: builder-refactor, docs-refactor, ux/frontend/backend-discovery-v2, folder-structure-v2.
- `docs/progress/sessions/`: fresh (this is the first new-cycle log).
- `docs/progress/index.csv`: header-only.
- `docs/archive/sessions/v0.3.4/`: 151 logs + index.csv preserved.
- Durable structural truth: `docs/product/decisions/src-structure-decision.md` (refreshed by P6) — the next FE/BE plans start here.

## Files
Files created:
  docs/archive/sessions/v0.3.4/ (151 logs + index.csv, archived copy)
  docs/progress/sessions/2026-06-28-claude/01-v0.3.4-archive-version-boundary.md (this log)
Files edited:
  docs/archive/README.md (v0.4.0 Archive entry, via archive-version.sh)
  docs/progress/index.csv (reset to header-only)
Files deleted:
  docs/progress/sessions/* (live copies — PRESERVED in archive, not lost)
  docs/plans/active/.DS_Store (macOS cruft)

Churn — work reversed: None. All logs preserved in archive.

Preserve-semantic check: No product code changed. Logs preserved (archived). Version number NOT changed (PO-only).

## DECISIONS FOR THE PO (blocking a clean v0.4.0 start)
1. **Version bump is PO-only (core.md §26).** `docs/VERSION.md` `current` is still `v0.3.4`; `next_planned` is `v0.4.0`. I did NOT bump it. To start the new plan as v0.4.0, the PO must state "the version is now v0.4.0" (then update only `VERSION.md current`).
2. **Version-source mismatch unresolved:** `docs/VERSION.md` (v0.3.4) vs `metadata.json` (v0.3.3, per build-current-state). Reconcile which is authoritative when bumping.

## Open issues / follow-ups
- PO: confirm v0.4.0 bump + reconcile VERSION.md/metadata.json.
- Next 4 plans only: FE-final-discovery → FE-final-implementation → BE-final-discovery → BE-final-implementation. They must read `docs/product/decisions/src-structure-decision.md` (current truth), not the SUPERSEDED v2 discovery outputs.
- Carried debt (from P6): `typed-any-cleanup` (42 any), `production-api-client-switch`, `P1b-color-tokens`, build-log-index.sh mislabel, sync-skills.sh plan-audit truncation, Quality-gates BLD ID.

index: hand-appended (build-log-index.sh has a known mislabel/dup bug; avoided a rebuild on the fresh index).
