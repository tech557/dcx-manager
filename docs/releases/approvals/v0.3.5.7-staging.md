---
version: v0.3.5.7
environment: staging
approved-by: PO (Mahmoud, tech@dotment.com)
approved-at: 2026-07-01
---

# Approval — v0.3.5.7 → staging

The PO explicitly approved this version for promotion to staging in-session, for the purpose of a real
end-to-end RG-R4 test of `promote.sh` (not a routine release). Target domain for this test is
`dcx-manager-gov-staging.vercel.app` (`PROMOTE_STAGING_DOMAIN` default) — **not** `staging.dcx.dotment.com`,
since that custom domain requires DNS access to `dotment.com` the PO has not yet granted (see RG-R0b/RG-R4
carry-forward).

This file is permanent record per RG-R2's design (`docs/releases/README.md`): never edited after sign-off;
a correction would be a new approval file, not an edit of this one.
