---
version: v0.3.5.0
environment: production
approved-by: PO (Mahmoud, tech@dotment.com)
approved-at: 2026-07-01
---

# Approval — v0.3.5.0 → production (first-production bootstrap, RG-R8)

The PO explicitly approved binding `v0.3.5.0` to the exact build described below and setting the
production alias for the first time, in-session:

> "Do you approve binding v0.3.5.0 ... to this exact build: commit b4b5d0b, Vercel deployment
> dcx-manager-dij3wundt-dot-techs-projects.vercel.app ... ?" — "Yes, approve v0.3.5.0 for production"

**Bound build:** commit `b4b5d0b09ed6f64f3cc45d76f9a376788def5707` on `integration` — the last real
(non-skipped) build at bootstrap time; every commit after it was docs-only and correctly skipped by
`vercel-ignore-build.sh` (RG-R7), so this is the artifact that represents current app content.

**Deployment:** `https://dcx-manager-dij3wundt-dot-techs-projects.vercel.app`
(`dpl_Fo3goq12YuLapcgin3Pq1Ms25cAR`), project `dcx-manager-gov`.

This is the one-time manual bootstrap per plan §3.2a — `v0.3.5.0` carries the pre-governance `v0.3.5`
baseline as-is (D-RG-VER); it is not a jump to `v0.4`. After this, all further production changes follow
the normal §2.3 promotion rules (no second bootstrap).

This file is permanent record per RG-R2's design: never edited after sign-off; a correction would be a
new approval file, not an edit of this one.
